/**
 * Root Layout for HerbisVeritas V2
 * Minimal root layout for i18n routing
 */
import "../src/styles/index.css"
import "../src/styles/prism-vsc-dark-plus.css"

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}